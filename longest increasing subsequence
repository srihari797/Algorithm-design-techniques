#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> arr={3, 2, 6, 4, 5};
    vector<int> dp(5,1);   // dp[i] = length of LIS ending at i
    
    for(int i=0;i<5;i++){
        for(int j=0;j<i;j++){
            if(arr[j] < arr[i]){
                dp[i] = max(dp[i], dp[j] + 1);
            }
        }
    }
    
    int max1 = *max_element(dp.begin(), dp.end());
    cout << max1;
}
